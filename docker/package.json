{
  "name": "docker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bundle": "esbuild ../lib/lambda/FunctionSpOrigin.ts --bundle --platform=node --outfile=sp.js",
    "build": "docker build -t bushibsp .",
    "run": "docker run -d --rm --name bushibsp -p 5000:5000 --env-file=.env bushibsp",
    "up": "npm run bundle && npm run build && npm run run"
  },
  "keywords": [],
  "author": "Warren Hennemuth",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.2",
    "express": "^4.18.2",
    "https": "^1.0.0",
    "node-forge": "^1.3.1"
  }
}
